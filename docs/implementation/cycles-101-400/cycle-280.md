# Improvement Cycle 280

## Topic

Integrate the adversarial purpose-formation thesis.

## Deficiency

Individual safeguards can be bypassed at another layer: secured references do
not stop source urgency manipulation, direct outcomes do not secure amendments,
and local authorization does not stop aggregate prohibition evasion. Partial
hardening leaves purpose formation as strong as its weakest unlinked boundary.

## Improvement

Added `validate_adversarial_purpose_formation_thesis` and an experimental
schema.

The gate links controls for signal incentives, reference authority,
beneficiary authenticity, downstream reporting, hidden harm, constitutional
amendment, aggregate action chains, ambiguity flooding, sensitive provenance,
and lineage commits. Eleven guarantees and all ten adversarial layers must hold
together. Evidence cannot authorize itself, and local compliance cannot
override aggregate harm.

## Scope boundary

Cycle 280 closes the adversarial section. Cycle 281 begins empirical
falsification with the smallest comparable system: one evolving signal case.

## Verification

- focused mission tests: 725 passed cumulatively
- schema JSON parse: passed
- `git diff --check`: passed

## Resulting invariant

Palamedes purpose formation is hardened only when evidence and authority remain
separate, interested sources are modeled, full consequences are evaluated,
and sensitive, concurrent lineage remains protected.
