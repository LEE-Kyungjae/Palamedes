# Improvement Cycle 220

## Topic

Integrate the contestable outcome-linked constitution thesis.

## Deficiency

Cycles 211–219 can exist as disconnected governance artifacts while execution
still bypasses conflict handling, contestation, or outcome correction. A
constitution is operational only when those states form one versioned path.

## Improvement

Added `validate_constitution_thesis_integration` and an experimental schema.

The gate binds the layer registry, contextual rule, precedence graph, override,
preference decay, precedent, authority grant, interpretation trace, audit, and
outcome record to one constitution version. It requires layered,
conflict-aware, versioned, contestable, and outcome-linked application.
Outcome links must cover clause application, override prediction, and authority
consequence, while interpretation traces remain non-certifying.

## Scope boundary

Cycle 220 completes the constitution thesis. Cycle 221 will begin world-model
governance by rejecting a single monolithic causal narrative.

## Verification

- focused mission tests: 485 passed cumulatively
- schema JSON parse: passed
- `git diff --check`: passed

## Resulting invariant

Palamedes executes values through a connected constitutional state whose
interpretation can be challenged and whose claims can be corrected by outcomes.
